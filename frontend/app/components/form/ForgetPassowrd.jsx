const ForgetPassowrd = ({ onClick }) => {
    return (
        <button
            className="bg-gray-800 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded-full focus:outline-none focus:shadow-outline"
            onClick={onClick}
        >
            Forgot Password?
        </button>
    )
}

export default ForgetPassowrd